import { SanitizeInput } from "./input_utils";

export function LookupRoll_ID(input: string){
    let roll_id: string = SanitizeInput(input);
    // inject loading html (?)
    // submit lookup request to api
    // on receiving api return, refresh page? 
    // (or redirect them to a page that's meant to receive lookup rolls)
}

export function LookupRoll_Name(input: string){
    let roll_name: string = SanitizeInput(input);
    // inject loading html (?)
    // submit lookup request to api
    // on receiving api return, refresh page? 
    // (or redirect them to a page that's meant to receive lookup rolls)
}

export function LookupRoll_URL(input: string){
    let roll_url: string = SanitizeInput(input);
    // inject loading html (?)
    // submit lookup request to api
    // on receiving api return, refresh page? 
    // (or redirect them to a page that's meant to receive lookup rolls)
}