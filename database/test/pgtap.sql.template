SET client_min_messages to warning;
CREATE EXTENSION IF NOT EXISTS pgtap;
RESET client_min_messages;

--- SET search_path = <insert schema>;

BEGIN;
--- insert plan statement

--- insert test statements

SELECT * FROM finish(true);
ROLLBACK;
